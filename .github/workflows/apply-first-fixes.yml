name: apply-first-fixes

on:
  push:
    branches:
      - agent/personal-brand-first-fixes

permissions:
  contents: write

jobs:
  apply-and-test:
    if: github.actor != 'github-actions[bot]'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: agent/personal-brand-first-fixes
          fetch-depth: 0

      - uses: actions/setup-node@v4
        with:
          node-version: 22

      - name: Apply approval guard and simplify Today
        run: |
          python3 - <<'PY'
          from pathlib import Path

          serve = Path('scripts/serve.mjs')
          text = serve.read_text()
          old