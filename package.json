{
  "name": "stuart-crowley-brand-engine",
  "version": "0.2.0",
  "private": true,
  "type": "module",
  "description": "Stuart Crowley's personal brand engine — intel ingestion, multi-channel content (LinkedIn / X / Substack and beyond), relationship intelligence, honest outreach and the Strait Up Growth pipeline. Local-first, zero dependencies; git is the database.",
  "scripts": {
    "dev": "node scripts/serve.mjs",
    "start": "node scripts/serve.mjs",
    "seed": "node scripts/seed.mjs",
    "seed:force": "node scripts/seed.mjs --force",
    "today": "node scripts/today.mjs",
    "ingest": "node scripts/ingest.mjs",
    "test": "node scripts/self-test.mjs"
  },
  "engines": { "node": ">=20" }
}
